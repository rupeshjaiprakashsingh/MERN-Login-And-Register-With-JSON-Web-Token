# Frontend API Configuration

# API Base URL - Set this to point to your backend server
# For cloud deployment, set this to your backend URL (e.g., https://api.example.com)
# If not set, defaults to http://localhost:VITE_API_PORT in development
# VITE_API_BASE=https://api.example.com

# Backend API Port - Used in development when VITE_API_BASE is not set
# Defaults to 3000 if not specified
VITE_API_PORT=3000
